buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.1.268
        stages:
          - name: tests
            steps:
              - command: make test
